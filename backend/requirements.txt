# --- MCP layer (this repo's contribution) ---
mcp>=2.0.0

# --- Product API layer ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
bcrypt>=4.1.0
pyjwt>=2.8.0
email-validator>=2.1.0

# --- LLM providers (from DATORYX) ---
requests>=2.31.0
numpy>=1.24.0
python-dotenv>=1.0.0

# --- market_analyst agent (from AI-Market-Analyzer) ---
pandas>=2.1.0
# yfinance>=0.2.40   # optional: only needed for fetch_symbol's live data pull

# --- Extended features: Docs/Slides/Sheets/Website generation ---
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0

# --- Deep Research (optional - web-grounded mode) ---
# Set TAVILY_API_KEY to enable real web search in /research; without it,
# /research still works but falls back to reasoning-only (see
# generators/research_engine.py). Tavily has a free tier.
# (No extra package needed - uses the `requests` dependency above.)

# --- Optional: local/offline models ---
# llama-cpp-python>=0.2.90   # for GGUF local models
# torch>=2.0.0               # for DATORYX's own native/from-scratch model (models/native/)

# --- Optional: DAXRO fine-tuned model (models/daxro_finetuned/) ---
# Qwen2.5-3B base + LoRA adapter. Requires a CUDA GPU (bitsandbytes 4-bit
# quantization does not run on CPU-only hosts) - only install/uncomment
# these on a GPU-backed deployment.
# torch>=2.0.0
# transformers>=4.40.0
# peft>=0.11.0
# bitsandbytes>=0.43.0
# accelerate>=0.30.0

# --- Core system ---
psutil>=5.9.0
